{
  "format": "pinmame-controller-profile",
  "version": 1,
  "id": "pinmame.wpc-security",
  "authority": "pinmame",
  "hardware_family": "Williams/Bally WPC Security",
  "inversion_applied_by_emulator": true,
  "groups": [
    {
      "id": "pinmame.input.switch",
      "direction": "input",
      "kind": "switch",
      "label": "Coin-door, switch-matrix, Fliptronic flipper, and optional custom switch column inputs",
      "address_rules": [
        { "minimum": 1, "maximum": 8 },
        { "minimum": 11, "maximum": 18 },
        { "minimum": 21, "maximum": 28 },
        { "minimum": 31, "maximum": 38 },
        { "minimum": 41, "maximum": 48 },
        { "minimum": 51, "maximum": 58 },
        { "minimum": 61, "maximum": 68 },
        { "minimum": 71, "maximum": 78 },
        { "minimum": 81, "maximum": 88 },
        { "minimum": 91, "maximum": 98 },
        { "minimum": 101, "maximum": 108 },
        { "minimum": 111, "maximum": 118 },
        { "minimum": 121, "maximum": 168 }
      ],
      "transports": {
        "libpinmame": { "channel": "switch" },
        "controller_plugin": { "group_id": 1, "contract_revision": "a6f6d7266d301249ed09c1b0123f8476513a2228" }
      },
      "notes": "#### Cabinet inputs\n\nAddresses `1-8` are the four coin chutes and the four coin-door service buttons in printed row order (internal array column `0`, `CORE_COINDOORSWCOL`).\n\n#### Matrix inputs\n\nPublic matrix addresses use WPC column/row notation `11-88`, where the left digit is the drive column (internal array index `1-8`) and the right digit is the return row `1-8`; `wpc_m2sw(col,row) = col*10+row+1`. Column indices `9` and `10` (public `91-98`/`101-108`) are a gap in the base allocation that this generation leaves unused unless a specific driver's own `getSol`/switch-read hook independently claims part of it.\n\n#### Fliptronic inputs\n\nThis generation always has Fliptronic flipper inputs at internal column `11` (`CORE_FLIPPERSWCOL`), so `111-118` are the eight EOS/button positions in `WPC_swF1..WPC_swF8` order:\n\n| Address | Position |\n| --- | --- |\n| `111` | lower-right EOS |\n| `112` | lower-right button |\n| `113` | lower-left EOS |\n| `114` | lower-left button |\n| `115` | upper-right EOS |\n| `116` | upper-right button |\n| `117` | upper-left EOS |\n| `118` | upper-left button |\n\nThese positions are exactly as on WPC-95 and WPC-DCS. A driver with no upper flippers is free to repurpose `115-118` for other switch-matrix-column-11 hardware entirely, or to leave the positions unwired.\n\n#### Custom switch columns\n\nA driver may declare one or more additional custom switch columns through `core_tGameData hw.swCol`. PinMAME's fixed custom-column start is internal array index `CORE_CUSTSWCOL = CORE_STDSWCOLS = 12` (`src/wpc/core.h`), one column beyond the Fliptronic column. The same `col*10+row+1` formula, reported through the `CORE_CUSTSWNO(c,r)` macro, places the declared columns as follows:\n\n| Declared custom column | Public addresses |\n| --- | --- |\n| First | `121-128` |\n| Second | `131-138` |\n\nFurther columns continue the same pattern. A definition declares only the addresses its own machine's custom board actually wires. This numbering is a fixed `core.h` offset, not a driver-visible printed silkscreen number. Any such printed board number must be captured as a `manual.address` alias rather than assumed equal to the public switch number.\n\n#### Normalization\n\nThis generation's `WPC_FLIPPERS` register read (`src/wpc/wpc.c`, `case WPC_FLIPPERS`, gated on `(gen & GENWPC_HASWPC95) == 0`, and `GENWPC_HASWPC95 = GEN_WPC95 | GEN_WPC95DCS` excludes `GEN_WPCSECURITY`) unconditionally returns the bitwise complement of the entire flipper switch matrix column exactly as on WPC-DCS and WPC-Fliptronic. The public Fliptronic state is already normalized here; a recreation must not re-invert it.\n\n#### Security-PIC row read\n\nSeparately, this generation's `WPC_SWROWREAD` register read (`src/wpc/wpc.c`, `case WPC_SWROWREAD`) is gated on `GENWPC_HASPIC = GEN_WPCSECURITY | GEN_WPC95 | GEN_WPC95DCS` and dispatches to the emulated security-PIC row reader (`wpc_pic_r`) instead of the plain `core_getSwCol` column reader that WPC-DCS/WPC-Fliptronic use. This changes how the emulator internally scans the matrix through the security handshake but does not change the public `col*10+row+1` address space or its normalization: `core_getSw`/`core_setSw`/`core_updInvSw` apply identically regardless of `GENWPC_HASPIC`.",
      "notes_format": "markdown"
    },
    {
      "id": "pinmame.input.dip",
      "direction": "configuration",
      "kind": "dip_switch",
      "label": "CPU-board option and country configuration bits",
      "address_rules": [{ "minimum": 1, "maximum": 8 }],
      "transports": { "libpinmame": { "channel": "dip" } }
    },
    {
      "id": "pinmame.output.solenoid",
      "direction": "controlled_device",
      "kind": "solenoid",
      "label": "Standard, state, Fliptronic, and game-specific custom outputs",
      "address_rules": [{ "minimum": 1, "maximum": 64 }],
      "transports": {
        "libpinmame": { "channel": "solenoid" },
        "controller_plugin": { "group_id": 1, "contract_revision": "a6f6d7266d301249ed09c1b0123f8476513a2228" }
      },
      "notes": "#### Standard and state outputs\n\nThis generation exposes standard drivers `1-28` (`src/wpc/core.c` `core_getSol`, `solNo <= 28`). Addresses `29-30` mirror two WPC J111 general-purpose GPIO bits, and `31-32` use the same remap (`core_getSol` `solNo <= 32` branch, \"Remap WPC GameOn/J111 GPIO\", gated on `GEN_ALLWPC`, not WPC95-specific). Of those, `31` is PinMAME's synthetic fast-flip game-on state taken from the driver's RAM flag when `wpc_set_fastflip_addr` is set, and `32` is normally zero.\n\n| Public addresses | Behavior |\n| --- | --- |\n| `33-36` | Driver-specific outputs at `solenoids2` bits `4-7` (`core_getSol` `solNo <= 36` branch). On a driver that sets `FLIP_SOL(FLIP_UR)`/`FLIP_SOL(FLIP_UL)`, they are the Fliptronic upper-flipper power/hold pairs: `33`/`34` upper-right and `35`/`36` upper-left. A driver with no upper flippers is free to drive ordinary coils through the same four addresses instead, with no address translation, exactly like WPC-DCS. |\n| `45-48` | Fliptronic lower-flipper circuits: `45`/`46` lower-right power/hold and `47`/`48` lower-left power/hold, identically addressed to WPC-95 and WPC-DCS. |\n| `49` | PinMAME's simulator-only ball-shooter channel. |\n| `50` | Reserved before the first custom-output boundary (`CORE_FIRSTCUSTSOL = 51`). |\n\n#### No LPDC range\n\nUnlike WPC-95/WPC-95DCS, this generation has no integrated LPDC board, so `37-44` are not a duplicated general-purpose output range. Pinned PinMAME's `core_getSol` dispatch (`src/wpc/core.c`, the \"37-44 WPC95 & S11 extra\" branch) only serves that range for `GEN_WPC95`/`GEN_WPC95DCS`/`GEN_ALLS11`, and returns constant `0` for this generation. Therefore `37-44` are simply unused address space here unless a specific driver's own `getSol` hook independently claims part of it.\n\n#### Custom outputs\n\nAbove that boundary, `51` upward are the game-specific custom solenoids a driver may publish through its own `core_tGameData.hw.getSol` handler, up to as many as its own `hw.custSol` count declares (`CORE_CUSTSOLNO(n) = CORE_FIRSTCUSTSOL - 1 + n`). A definition declares only the addresses its own machine publishes.\n\nA driver's own custom-solenoid board may carry silkscreened item numbers that do not match this public address space. Any such printed manual item number must be captured as a `manual.address` alias rather than assumed equal to the public solenoid number.",
      "notes_format": "markdown"
    },
    {
      "id": "pinmame.output.lamp",
      "direction": "controlled_device",
      "kind": "lamp",
      "label": "Eight-by-eight controlled-lamp matrix with optional auxiliary columns",
      "address_rules": [
        { "minimum": 11, "maximum": 18 },
        { "minimum": 21, "maximum": 28 },
        { "minimum": 31, "maximum": 38 },
        { "minimum": 41, "maximum": 48 },
        { "minimum": 51, "maximum": 58 },
        { "minimum": 61, "maximum": 68 },
        { "minimum": 71, "maximum": 78 },
        { "minimum": 81, "maximum": 88 },
        { "minimum": 91, "maximum": 98 },
        { "minimum": 101, "maximum": 108 }
      ],
      "transports": {
        "libpinmame": { "channel": "lamp" },
        "controller_plugin": { "group_id": 512, "contract_revision": "a6f6d7266d301249ed09c1b0123f8476513a2228" }
      },
      "notes": "PinMAME applies the shared wpc_sw2m/wpc_m2sw conversion to lamps (src/wpc/wpc.c MDRV_LAMP_CONV). ChangedLamps therefore reports public matrix addresses 11-88 rather than sequential indices, where the left digit is the drive column and the right digit is the return row. A driver may declare additional lamp columns through core_tGameData hw.lampCol; a definition declares only the columns its own machine populates."
    },
    {
      "id": "pinmame.output.gi",
      "direction": "controlled_device",
      "kind": "gi",
      "label": "Five general-illumination strings",
      "address_rules": [{ "minimum": 0, "maximum": 4 }],
      "transports": {
        "libpinmame": { "channel": "gi" },
        "controller_plugin": { "group_id": 256, "contract_revision": "a6f6d7266d301249ed09c1b0123f8476513a2228" }
      },
      "notes": "Public GI addresses are zero-based 0-4 and correspond to the manuals' printed strings 01-05."
    }
  ],
  "sources": [
    {
      "uri": "https://github.com/vpinball/pinmame",
      "revision": "4ec52ff0ac133ac251681518aed2249e19fe26eb",
      "locator": "src/wpc/gen.h GEN_WPCSECURITY=U64(0x20) 'Security chip, World Cup Soccer 3/94 - Jackbot 10/95'; src/wpc/wpc.c GENWPC_HASFLIPTRON=(GEN_ALLWPC & ~(GEN_WPCALPHA_1|GEN_WPCALPHA_2|GEN_WPCDMD)) which includes GEN_WPCSECURITY; GENWPC_HASWPC95=(GEN_WPC95|GEN_WPC95DCS) which excludes GEN_WPCSECURITY; GENWPC_HASPIC=(GEN_WPCSECURITY|GEN_WPC95|GEN_WPC95DCS); wpc_r case WPC_FLIPPERS unconditional complement of coreGlobals.swMatrix[CORE_FLIPPERSWCOL] for (gen & GENWPC_HASWPC95)==0; wpc_r case WPC_SWROWREAD dispatches to wpc_pic_r() when GENWPC_HASPIC; wpc_w case WPC_SWCOLSELECT dispatches to wpc_pic_w() when GENWPC_HASPIC; src/wpc/core.h CORE_COINDOORSWCOL=0, CORE_FLIPPERSWCOL=11, CORE_STDSWCOLS=12, CORE_CUSTSWCOL=CORE_STDSWCOLS, CORE_CUSTSWNO(c,r)=((CORE_CUSTSWCOL-1+c)*10+r), CORE_FIRSTUFLIPSOL=33, CORE_FIRSTLFLIPSOL=45, CORE_FIRSTCUSTSOL=51, CORE_CUSTSOLNO(n)=CORE_FIRSTCUSTSOL-1+n, CORE_STDLAMPCOLS=8, CORE_CUSTLAMPCOL=CORE_STDLAMPCOLS; src/wpc/core.c core_getSol full dispatch (29-32 GEN_ALLWPC J111/GameOn remap, 33-36 driver-specific solenoids2 bits4-7 unless FLIP_SOL(FLIP_UR)/FLIP_SOL(FLIP_UL) is set, 37-44 branch gated on GEN_WPC95/GEN_WPC95DCS/GEN_ALLS11 only, 45-48 lower flippers, 49-50 sim_getSol, 51+ hw.getSol); src/wpc/core.c core_getSw/core_setSw/core_updInvSw (swNo=(swNo/10)*8+(swNo%10-1); invSw[swNo/8] applies regardless of GENWPC_HASPIC); src/libpinmame/libpinmame.h PINMAME_HARDWARE_GEN_WPCSECURITY=0x20"
    },
    {
      "uri": "https://github.com/vpinball/pinmame",
      "revision": "a6f6d7266d301249ed09c1b0123f8476513a2228",
      "locator": "src/libpinmame/libpinmame.cpp SetupMsgApi group definitions"
    }
  ]
}
