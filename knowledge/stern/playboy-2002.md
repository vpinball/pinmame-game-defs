# Playboy (Stern 2002)

Coverage: **partial - machine identity only. Nothing about playfield devices, wiring, mechanisms,
or behavior is evidenced yet.**

This record was promoted from the generated catalog stub `stub.pinmame.playboys` by the
catalog-wide identity pass of 2026-08-29. The promotion resolves machine identity and carries the
catalog's residual driver grouping over unchanged, and it deliberately asserts nothing else. Every
requirement in the definition's `coverage.missing` is genuinely outstanding.

## Identity

- PinMAME catalog: root driver `playboys`, description "Playboy (Stern, 5.00)", manufacturer
  "Stern", catalog year "2002".
- OPDB record `GrPKO-MLO5X` (IPDB 4506) names this machine "Playboy"
  (Stern, manufacture date 2002-01-01); the resolved identity rests on the
  agreement of the PinMAME catalog and this reviewed mapping.
- The definition's driver list is exactly the clone tree PinMAME declares under `playboys`;
  whether every listed driver really runs on this physical machine is unverified.

## Drivers this record holds

- `play203` (2002, Stern, clone of `playboys`).
- `play203f` (2002, Stern, clone of `playboys`).
- `play203g` (2002, Stern, clone of `playboys`).
- `play203i` (2002, Stern, clone of `playboys`).
- `play203l` (2002, Stern, clone of `playboys`).
- `play300` (2002, Stern, clone of `playboys`).
- `play302` (2002, Stern, clone of `playboys`).
- `play302f` (2002, Stern, clone of `playboys`).
- `play302g` (2002, Stern, clone of `playboys`).
- `play302i` (2002, Stern, clone of `playboys`).
- `play302l` (2002, Stern, clone of `playboys`).
- `play303` (2002, Stern, clone of `playboys`).
- `play303f` (2002, Stern, clone of `playboys`).
- `play303g` (2002, Stern, clone of `playboys`).
- `play303i` (2002, Stern, clone of `playboys`).
- `play303l` (2002, Stern, clone of `playboys`).
- `play401` (2002, Stern, clone of `playboys`).
- `play401f` (2002, Stern, clone of `playboys`).
- `play401g` (2002, Stern, clone of `playboys`).
- `play401i` (2002, Stern, clone of `playboys`).
- `play401l` (2002, Stern, clone of `playboys`).
- `playboyf` (2002, Stern, clone of `playboys`).
- `playboyg` (2002, Stern, clone of `playboys`).
- `playboyi` (2002, Stern, clone of `playboys`).
- `playboyl` (2002, Stern, clone of `playboys`).
- `playboys` (2002, Stern).

## What a curator must establish next

Controller platform from the pinned PinMAME driver source; full input, output, and display
enumeration with semantic names; physical wiring and polarity; mechanism inventory and behavior;
variant differences across the clone tree; recreation knowledge from a manual, schematic, or
known-working table; runtime provenance; and a normalized spatial placement for every physical
device. No manual, table, ROM analysis, or harness evidence is retained for this machine yet.
