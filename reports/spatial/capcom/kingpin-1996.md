# Capcom Kingpin (1996) spatial blockers

Generated by `tools/curate_kingpin.py`. Status: **observed**; `spatial_placement` remains in `coverage.missing`.

spatial_placement stays in coverage.missing: every coordinate comes from one unverified community lineage, flasher 22 has only a wide glow light with no flasher dome, flasher 29's orbit side is conflicted, several mechanism devices have no usable table object, and some multi-bulb outputs have fewer placements than bulbs.

## Source

- Table: `Kingpin (Capcom 1996) SG1bsoN - 1920 Mod.vpx`, SHA-256 `9f26df43bcfac2ca8f47f75ebefa87e6f40d56029242004d528fab8cee5d37cf` (106242048 bytes), source `vpx-table.kingpin-sg1bson-1920-mod-1.1`.
- Script: SHA-256 `e12c314213227337c7ebd78e8789dcdc65af34e8b385278e5ee6f1a26f6fdc3d`, source `vpx.kingpin-sg1bson-1920-mod-1.1`.
- Extraction: vpxtool git:v0.33.3, 1463 files, 172289469 bytes, canonical manifest SHA-256 `5f8485fb81afcef48f42f16974d49d944bd078e69cd0ce198c5c2fac29082b60`.
- Bounds: left 0.0, top 0.0, right 952.0, bottom 2162.0.
- Transformation: x = (object x - left) / (right - left), y = (object y - top) / (bottom - top), rounded to six decimals; each point is one table object's own centre (a wall's is the centroid of its outline points).

## Selection rules

- Switches and coils use the object the table script binds to the address, or the mechanism object named in the device's projection note. A Wall has no centre of its own; its placement is the centroid of its outline points (listed under centroid_placements).
- Lamps use the table's l<N>a16 playfield insert light when it exists; otherwise the script-bound lights, without the two whole-table colour-grade helpers (falloff >= 1000), are grouped within 0.05 normalized units and each group is placed at its smallest-falloff light.
- Flashers use the lights the table script drives from the address, directly or through its flash routines' pseudo-lamps (18 via 130, 23 via 129, 27 via 131), except where Krellan's hands-on chart and the ROM name contradict the table's side (19 and 24).
- Backpanel lamps 116, 119 and 120 lie behind the playfield's rear edge in the table and are clamped to y = 0.

## Counts

179 devices carry 186 observed placements; 10 used devices are not placed.

## Projections and substitutions

| Device | Note |
| --- | --- |
| `switch.32` | the table's captive-ball target object (its handler pulses 49, a table defect; the object is the captive-ball target) |
| `switch.33` | projected onto the left flipper's pivot: the end-of-stroke switch is part of the flipper assembly and the table has no object for it |
| `switch.34` | projected onto the right flipper's pivot: the end-of-stroke switch is part of the flipper assembly and the table has no object for it |
| `switch.36` | projected onto the trough exit kicker: the table models the four-ball trough as a script ball stack with no switch objects |
| `switch.37` | projected onto the trough exit kicker: the table models the four-ball trough as a script ball stack with no switch objects |
| `switch.38` | projected onto the trough exit kicker: the table models the four-ball trough as a script ball stack with no switch objects |
| `switch.39` | projected onto the trough exit kicker: the table models the four-ball trough as a script ball stack with no switch objects |
| `switch.52` | projected onto the slot drum primitive: the drum's index opto has no table object |
| `solenoid.9` | the flipper's pivot |
| `solenoid.10` | the flipper's pivot |
| `solenoid.12` | the slot drum primitive the motor turns |
| `solenoid.13` | the invisible collision gate the table's LoopGate routine toggles, the only gate it models for the ROM's TOPGATES; it sits behind and to the left of the top lanes, where Krellan puts the top diverter that blocks the left orbit |
| `solenoid.15` | the bumper the ROM names C. STAR BUMPER (Krellan: bottom jet bumper); VPX fires bumpers without a coil callback |
| `solenoid.16` | the bumper the ROM names R. STAR BUMPER; VPX fires bumpers without a coil callback |
| `solenoid.17` | the bumper the ROM names L. STAR BUMPER; VPX fires bumpers without a coil callback |
| `solenoid.32` | the shooter-lane trigger the table's impulse plunger acts on (InitImpulseP swplunger), which its auto-fire routine fires |
| `solenoid.18` | the light the table's LeftRampFlash routine (SolCallback 18) drives through pseudo-lamp 130, on the left ramp's upper curve under the table's flasher dome mesh (Primitive_flasherdome, centred there once its size and position are applied); the script's own comment calls it right ramp flash |
| `solenoid.19` | Krellan puts 19 under 'Sudden' by the left flipper and the ROM names it Left Kid; the table binds its left-side flasher object F24 to 24 and F19, on the right, to 19 (a table defect), so 19 takes F24's position |
| `solenoid.23` | the light the table's RightRampFlash routine (SolCallback 23) drives through pseudo-lamp 129, beside the Hotel Lex building under the table's red dome mesh (Hotel_RedFlasher, centred there once its size and position are applied); the script's own comment calls it hotel lex flash |
| `solenoid.24` | Krellan puts 24 under 'Death' by the right flipper and the ROM names it Right Kid; the table binds its right-side flasher object F19 to 19 (a table defect), so 24 takes F19's position |
| `solenoid.27` | the table drives two lights from 27 through pseudo-lamp 131, one at each end of the power meter; the ROM calls the output Power Flashers |

## Wall centroids

`solenoid.4`, `solenoid.5`, `switch.25`, `switch.26`, `switch.27`, `switch.28`, `switch.29`, `switch.30`, `switch.31`, `switch.32`, `switch.41`, `switch.42`, `switch.49`, `switch.50`, `switch.60`, `switch.63`

## Backpanel clamps

- `lamp.116`: raw normalized y -0.040053, placed at y = 0
- `lamp.119`: raw normalized y -0.040284, placed at y = 0
- `lamp.120`: raw normalized y -0.053929, placed at y = 0

## Fewer placements than bulbs

- `solenoid.18`: 1 placement(s); the ROM's plural name 'L. RAMP FLASHERS'; the table models one light
- `solenoid.21`: 1 placement(s); the ROM's plural name 'GUN TIP FLASHERS'; the table models one light
- `solenoid.30`: 1 placement(s); the ROM's plural name 'KING FLASHERS'; the table models one light
- `solenoid.31`: 1 placement(s); the ROM's plural name 'PIN FLASHERS'; the table models one light
- `lamp.97`: 1 placement(s); the ROM's '(2)'; the table binds one light
- `lamp.98`: 1 placement(s); the ROM's '(2)'; the table binds one light
- `lamp.117`: 1 placement(s); the ROM's '(2)'; the table binds one light
- `lamp.118`: 1 placement(s); the ROM's '(2)'; the table binds one light
- `lamp.127`: 1 placement(s); the ROM's '(2)'; the table binds one light

## Candidates not placed

- `lamp.127`: the table's l127c light at (0.834, 0.080), behind the upper-right standup, fits Krellan's 'behind right gun standup and at corner between that lane and right orbit, x2' for this output, but the table script comments its binding out; it is a candidate for the output's second bulb and is not placed

Derivation scripts (`external:pinmame-review-artifacts/kingpin/spatial`): bindings.py `c34ecf08b4feb813857421ceabc76f3bbc6738fd3f42acd0254bf0c9ae142a19`, bindings.json `0984d4fe9ec9eb502f58db76cefe976513355ac175e729dfc981460b0f3e5f22`, placements.py `fdd7dac0c7e1a0b14983b7e86cec91c60f9d74e64019a80fa846d0a7d3617dae`, placements.json `4c264e79cb6e1e3093e536274e0878b693ed9956efaf86c3df10f8deb37a7785`, emit_curator_block.py `74bb9257191cf26cca8d431bdcc6441cd65956547570c0e1fdbaad303c27d1d7`.

## Not placed

| Device | Reason |
| --- | --- |
| `switch.45` | the table models the three-ball gun lock as a script ball stack with no switch objects; Krellan puts gun lock 1 in the hideout under the ramp, and in the table the lock ends at the gun eject near the left orbit, so no source places gun lock 2 |
| `switch.46` | the table models the three-ball gun lock as a script ball stack with no switch objects; Krellan puts gun lock 1 in the hideout under the ramp, and in the table the lock ends at the gun eject near the left orbit, so no source places gun lock 3 |
| `switch.47` | the table raises the ramp with an invisible helper flipper parked beyond the table edge (LeftRampFlipper, x > 1) and has no object for the ramp-down switch |
| `switch.48` | no table object models the gun trough opto, and no source locates it |
| `solenoid.3` | the knocker has no table object and no source locates it |
| `solenoid.6` | the drop-target reset coil sits under the KING bank; the table has no single object for it and a centroid of the four targets would be invented |
| `solenoid.7` | the drop-target reset coil sits under the PIN bank; the table has no single object for it and a centroid of the three targets would be invented |
| `solenoid.14` | the table raises the ramp with an invisible helper flipper parked beyond the table edge (LeftRampFlipper, x > 1) |
| `solenoid.22` | the table drives only f22 from it, a light near the right ramp's exit with an 800 falloff (every other flasher light in the table has 300 or less) and no flasher dome at it, and no source locates the physical bulb |
| `solenoid.29` | its orbit side is the open conflict conflict.flasher-29-orbit-side |
